import React from "react";

const SidebarLeft = (props) => {
    return (
        <aside id="sidebarLeft">
            <div className={`f`}>
                dsasfd
            </div>
        </aside>
    )
};

export default SidebarLeft;